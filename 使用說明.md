# 自然饗宴｜教學網　使用說明

語文　115 學年度 第 1 學期　第 4 單元　第 13～16 週

## 這是什麼

把〈自然饗宴〉整個單元的教材做成一個**純靜態網站**，雙擊 `index.html` 就能用，
不必安裝、不必連網（只有影片與 PDF 是本機檔案，一樣離線可用）。

## 兩種開啟方式

| 開法 | 版本 | 差別 |
|---|---|---|
| 雙擊本資料夾的 `index.html` | **教室版** | 點名區可以上傳學生照片 |
| 用線上網址開啟 | **公開版** | 照片上傳自動關閉，只能輸入姓名 |

學生姓名存在瀏覽器的 localStorage、照片存 IndexedDB，**只留在那台電腦，不會上傳到網路，也不在 GitHub 裡**。

## 六個分區

| 分區 | 內容 |
|---|---|
| 🙋 點點名 | 抽籤點名，一輪每人只點一次，名單可匯出匯入 |
| 🃏 圖詞輪播 | 10 張學習圖卡，詞與說明各自可單獨發音 |
| 🎬 教學影片 | 主影片 1 支＋主題短片 3 支，右邊清單可自由切換 |
| 📚 教材教學 | 課文與閱讀練習（P01–P20）→ 四個重點，每個重點教學頁後接 3 題形成性評量 |
| 📝 評量區 | 12 題集中做一次，做完顯示各重點正確率；另可開學習單、練習單、圖卡與完整簡報 PDF |
| 🧠 自學區 | 圖卡牆，點一下放大，可自動輪播 |

側邊還有 🖊️ 畫筆與 ⏱️ 三組獨立計時器。

## 教材內容

- 頁圖 **81 頁**（教材 PPTX、PDF、紙本都由這 81 張產生）
- 四個重點：讀懂臺灣山脈地圖／讀懂行前準備與安全／讀懂山林生態與保育／情境練習與生活遷移
- 正式評量 **12 題**兩選一，正解序列 `ABBAABABBABA`（A、B 各 6 題）
- 學習圖卡 **10 張**
- 語音 **97 支**（`zh-TW-HsiaoChenNeural`，語速 -25%）
- 影片 **4 支**：教學影片（字幕版）、臺灣山脈、行前安全準備、守護山林

## 要更新內容時

**`語文_第一學期_第4單元_自然饗宴\pages\` 才是教材本體**，其餘（PPTX、PDF、紙本、影片、本網站）全是衍生檔。

```
1. python 重生頁圖.py p41            # 只重生要改的那一張
2. 逐字校對 → python 套用重生圖.py p41   # 通過才覆蓋 pages\
3. 重跑下游：重出簡報.mjs → 重建紙本.py →（影片有引用到才）重建教學影片.py
4. python 內容稽核.py <單元> --full   # ③④⑦ 要逐張讀圖
5. 網站：工具腳本\建置素材_圖片壓縮.py →（改到題目或口播才要）建置語音_全部.py
6. 跳版本號 → git commit → git push
```

🛑 **改了 `教學網站\` 裡的任何東西，`index.html` 的 `?v=` 就要加 1**。
`資源版本` 與 `語音.版本` 都住在 `config.js` 裡，而 `config.js` 本身也是用 `?v=` 載入的——
只改它們而不跳 `index.html` 的 `?v=`，回訪者會拿到快取裡的舊 `config.js`，等於沒更新。

## 題目的唯一來源

題目、選項與正解的唯一來源是素材資料夾的 `題庫.py`。
網站的 `js/data.js`、語音腳本用的 `工具腳本\語音資料.py`（由 `data.js` 自動產生）
與紙本 `重建紙本.py` 都要跟它一致，改完跑：

```
python 工具腳本/產生語音資料.py
python 工具腳本/跨檔一致性.py
```
